class Solution {
    public void sortColors(int[] nums) {
        Arrays.sort(nums);
    }
}// tc -> O(nlogn) sc -> O(1) but this technique will be restricted in many platforms so we can use 2 pointer approach to solve this problem in O(n) time and O(1) space
